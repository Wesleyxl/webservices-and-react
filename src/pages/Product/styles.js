import styled from 'styled-components';

export const Container = styled.div`
    max-width: 1300px;
    margin: 0 auto;
    width: 100%;
    min-height: calc(100vh - 160px);
    display: flex;
    flex-wrap: wrap;
    padding: 15px;
`;

